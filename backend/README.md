# astrovaani

# Astrology AI Backend (FastAPI) + OpenRouter + Supabase

## Setup

1. Create a Python virtual env and install:
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
